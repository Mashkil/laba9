#include <stdio.h>

int main()
{
	int K = 99; // Задаем день
	int N = 5; // Задаем первый день недели в году
	while (K > 7) // До тех пор, пока значение больше 7, вычитаем по одной неделе.
		K -= 7;
	K = K + 6; // Также учитываем, что год начался с 7 дня недели.
	printf_s("%i", K);
	return 0;
}
